# Area LP Generator - Cursor Status Report

**Generated:** $(date +"%Y-%m-%d %H:%M:%S")  
**Branch:** save/working-20251101-1357  
**Root:** /Users/kinotomonari/claude_files/area-guides

## 進捗サマリ

| Slug | Status | MDX有無 | 欠落キー数 | 最終更新 | 言語 |
|------|--------|--------|------------|----------|------|
| shibuya | Published | ✅ | 22 | - | enのみ |

## 現状理解

### ワークスペース状況
- **Node.js:** v18.20.7
- **pnpm:** 10.14.0
- **Next.js:** 15.2.4
- **Git状態:** 1ファイル変更あり（dirty）
- **MDX設定:** `@next/mdx`設定済み、`pageExtensions`に`md/mdx`追加済み

### エリア状況
- **総エリア数:** 1
- **Published:** 1
- **Draft:** 0

### shibuya エリアの詳細
- **YAML:** `content/area/shibuya.yml` (存在)
- **MDX:** `app/(marketing)/areas/shibuya/page.mdx` (存在)
- **状態:** Published
- **言語:** enのみ（ja未対応）

### 欠落キー（shibuya.yml）
現在のYAMLは最小限のスキーマ（`AreaFrontmatter`）のみ。拡張スキーマ（`area-variables.schema.json`）に合わせて以下が欠落：

**必須キー:**
- `locale` (配列形式: `["en","ja"]`)
- `title.en` / `title.ja` (オブジェクト形式)
- `rent_by_type.quarter`
- `rent_by_type.*` (1K, 1LDK等)
- `commute_times` (配列)
- `subareas` (配列)
- `cta.text.en` / `cta.text.ja` / `cta.url`
- `sources` (配列)
- `last_updated` (ISO日付)

**推奨キー:**
- `subtitle.en` / `subtitle.ja`
- `overview.en` / `overview.ja`
- `who_it_fits.en` / `who_it_fits.ja`
- `schools_healthcare.en` / `schools_healthcare.ja`
- `parks.en` / `parks.ja`
- `shopping_dining.en` / `shopping_dining.ja`
- `events` (配列)

## やること（Next Actions）

1. **shibuya.yml の拡張**
   - 拡張スキーマ（`bmad/prompts/area-variables.schema.json`）に合わせて更新
   - 必須キーを全て追加
   - 数値は変数YAMLのみを出所とする

2. **MDXページの完全化**
   - Heroセクション（title + subtitle + as-of tag）
   - 目次（TOC）自動生成
   - 全セクション追加：
     - Overview
     - Who it fits
     - Subareas
     - Commute times
     - Rents (quarter表示)
     - Schools & Healthcare
     - Parks
     - Shopping & Dining
     - Events
     - FAQ
   - CTAを2箇所配置（ページ上部と下部）

3. **日本語対応**
   - 全てのセクションに`ja`ロケールを追加
   - i18nブロック構造を実装

4. **データソースの明記**
   - `sources`配列にSUUMO/Homes等を追加
   - データの出所を明確化

## PRD受入基準チェック

- ✅ `/areas/shibuya` でMDXページ存在
- ⚠️ Hero+TOC+CTA表示 → MDXは最小構成（拡張必要）
- ✅ Draftは一覧非表示（shibuyaはPublished）
- ⚠️ `pnpm dev` でビルドエラー → MDXコンポーネントエラー可能性あり

## 注意事項

- **EN/JAのみ対応。** 他言語は出力しない。
- **CTAは上下2箇所必須。** ページ上部と下部に配置。
- **数値は変数YAMLのみを出所とする。** 無い値は書かない。
- **既存ファイルは上書き禁止。** 提案ファイルのみ作成。

## 次に貼るべきプロンプト

`/start shibuya` の後に拡張スキーマ準拠のJSONを提供して、YAML+MDX生成を実行
